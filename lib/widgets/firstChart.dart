class FirstChart{
  late int maxPackage;
  late String year;

  FirstChart({required this.maxPackage, required this.year});

  // factory FirstChart.fromJson( Map<String,int> data)=>
  //     FirstChart(maxPackage: data['placementCount']!);
}